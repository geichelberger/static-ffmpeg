version: 2
jobs:
  build:
    docker:
      - image: alpine:edge
    steps:
      - checkout
      - run:
          name: Installing dependencies
          command: ./install_deps_alpine_docker.sh
      - run:
          name: Get sources
          command: ./get_sources.sh
      - run:
          name: Building
          no_output_timeout: 60m
          command: ./build_ffmpeg.sh
      - run:
          name: Deploy
          command: ./deploy.sh
workflows:
  version: 2
  commit:
    jobs:
      - build
  nightly:
    triggers:
      - schedule:
          cron: "50 3 * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - build
